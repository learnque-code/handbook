{
  "scripts": {
    "watch": "cross-env NODE_ENV=development run-s build:watch",
    "build": "cross-env NODE_ENV=production run-s build:prod",
    "build:prod": "snowpack build",
    "build:watch": "snowpack build --watch",
    "clean": "rm -rf ../resources/static/*",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.{ts,js}\"",
    "lint": "prettier --check \"src/**/*.{ts,js}\""
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "postcss-flexbugs-fixes": "^5.0.2",
    "stimulus": "^2.0.0",
    "tailwindcss": "^2.0.3"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^4.0.2",
    "@snowpack/plugin-postcss": "^1.1.0",
    "@snowpack/plugin-webpack": "^2.3.0",
    "autoprefixer": "^10.2.5",
    "cssnano": "^4.1.10",
    "postcss": "^8.2.7",
    "postcss-cli": "^8.3.1",
    "postcss-import": "^14.0.0",
    "postcss-preset-env": "^6.7.0",
    "prettier": "^2.2.1",
    "snowpack": "^3.0.11"
  }
}
